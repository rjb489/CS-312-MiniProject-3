<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/style.css" rel="stylesheet">
    <title>Edit Blog Post</title>
</head>
<body>
    <h2>Edit Blog Post</h2>

    <!-- The form action should point to the updated route and pass the postID -->
    <form action="/edit/<%= postID %>" method="POST">
      <label for="title">Title:</label>
      <input type="text" name="title" value="<%= blogPost.title %>"><br><br>
  
      <label for="author">Author:</label>
      <input type="text" name="author" value="<%= blogPost.creator_name %>"><br><br>
  
      <label for="body">Content:</label><br>
      <textarea name="body" cols="50" rows="10"><%= blogPost.body %></textarea><br><br> <!-- Ensure this name is 'body' -->
  
      <button type="submit">Save Changes</button>
  </form>
  

    <br>
    <a href="/blogs">Back to Home</a>
</body>
</html>
